# Upstream Docker Compose
# TODO: Phase 2 will define all services

version: "3.9"

services:
  placeholder:
    image: alpine:3.19
    command: ["echo", "Upstream skeleton ready. Phase 2 will replace this."]
